name = "telegram-bot-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# trigger: 2026-07-04 — auto-deploy on push to main
# Секрет в wrangler (можно тот же, что TELEGRAM_RELAY_SECRET):
#   npx wrangler secret put TELEGRAM_BOT_PROXY_SECRET

[observability]
enabled = true
