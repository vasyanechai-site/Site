> **Сводка по проекту (май 2026):** данные и заказы — **Node API** (`server/`), фронт в проде — только **`dist/`** после `npm run build` (в `index.html` — скрипты `/assets/*.js`, не `/src/main.tsx`, иначе «белый экран»). Актуальные инструкции: в корне **`README.md`**, **`НАЧАЛО-БЕЗ-КОДА.md`**, **`ПРОДАКШН-100-ПРОЦЕНТОВ.md`**, **`DEPLOY_REGRU.md`**; быстрый старт во фронте — **`src/app/START_HERE.md`**.
>
> _Ниже — архивная или тематическая заметка; шаги, где основной бэкенд описан только как Supabase Edge без Node, для текущего продакшена могут быть неполными._

# ✅ Чеклист миграции на coffeenechai.ru

## 🎯 Что уже сделано в коде

Все упоминания домена `optnechai.ru` заменены на `coffeenechai.ru`:

### ✅ Критичные изменения (выполнено):
- [x] **Tochka платежи** - redirectUrl и failRedirectUrl для розничных заказов
- [x] **Email уведомления** - ссылки для входа в аккаунт в письмах регистрации
- [x] **Email адреса** - `no-reply@coffeenechai.ru` и `support@coffeenechai.ru`
- [x] **Юридические документы** - упоминания домена в PrivacyPolicy, UserAgreement, MarketingConsent
- [x] **Счета Tochka** - комментарии в счетах на оплату
- [x] **Документация** - примеры в README файлах

---

## 📋 Что нужно сделать вручную

### 1️⃣ Настройки Supabase (ОБЯЗАТЕЛЬНО!)

**Где:** Supabase Dashboard → Authentication → URL Configuration

Обновите следующие параметры:
```
Site URL: https://coffeenechai.ru
```

Добавьте в **Redirect URLs:**
```
https://coffeenechai.ru/**
https://coffeenechai.ru/auth/callback
```

⚠️ **Без этого не будет работать:** регистрация и вход розничных пользователей через email.

---

### 2️⃣ Настройка DNS

1. Укажите A-запись или CNAME для `coffeenechai.ru` на ваш сервер/хостинг
2. Настройте SSL-сертификат для нового домена
3. (Опционально) Настройте редирект с `optnechai.ru` на `coffeenechai.ru`

---

### 3️⃣ Email сервис

Если используете корпоративную почту:
- Создайте `no-reply@coffeenechai.ru` для системных писем
- Создайте `support@coffeenechai.ru` для поддержки
- Обновите настройки в Resend (если меняете отправителя)

**Где:** Код использует Resend API, настройки в переменных окружения `RESEND_API_KEY`

---

### 4️⃣ Проверка после миграции

После переноса на новый домен **обязательно протестируйте:**

#### Розница:
- [ ] Регистрация нового пользователя → получение письма → вход по ссылке
- [ ] Оформление заказа → оплата → редирект на страницу успеха
- [ ] Проверка, что после оплаты пользователь попадает на `coffeenechai.ru/payment/success`

#### Опт:
- [ ] Вход по логину/паролю
- [ ] Создание заказа и получение счета
- [ ] Telegram уведомления о новых заказах

#### Общее:
- [ ] СДЭК калькулятор и выбор ПВЗ
- [ ] Бегущая строка на главной
- [ ] Избранное и корзина

---

## 🔧 Технические детали

### Что работает автоматически (не требует изменений):

✅ **СДЭК** - использует только API, не зависит от домена  
✅ **Telegram** - webhook URL формируется динамически из `SUPABASE_URL`  
✅ **Supabase Edge Functions** - используют `projectId` из переменных окружения  
✅ **Внешние ресурсы** - изображения с tildacdn, static ресурсы

### Переменные окружения (не требуют изменений):

Все переменные остаются прежними:
- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`
- `TOCHKA_JWT_TOKEN`
- `TOCHKA_CUSTOMER_CODE`
- `RESEND_API_KEY`

---

## ⚠️ Важные напоминания

1. **Не забудьте обновить Site URL в Supabase** - это критично для аутентификации!
2. **Протестируйте оплату** - убедитесь, что редирект работает правильно
3. **Проверьте письма** - они должны содержать ссылки на новый домен
4. **Telegram останется работать** - webhook обновится автоматически при первом запросе

---

## 📞 В случае проблем

Если после миграции что-то не работает:

1. **Проверьте логи Supabase Functions** - там будут ошибки API
2. **Проверьте консоль браузера** - ошибки при оплате и редиректах
3. **Проверьте Site URL в Supabase** - частая причина проблем с auth

---

**Дата миграции:** _________  
**Статус:** Код обновлен ✅ | Настройки Supabase: ⬜ | DNS: ⬜ | Email: ⬜ | Тестирование: ⬜
